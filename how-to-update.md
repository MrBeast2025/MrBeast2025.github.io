# 如何更新内容（保姆级）

> 日常修改 90% 都在 `content.js` 这一个文件里。
> 改之前建议先复制一份 `content.js` 备份（改坏了可以直接还原）。

---

## 准备：怎么打开、怎么看效果

1. 打开文件夹 `D:\1deepseek Harness\2杂乱\个人网站\`；
2. 右键 `content.js` → **打开方式** → 记事本（或 VS Code）；
3. 改完 **Ctrl + S** 保存；
4. 双击 `index.html`，浏览器里看效果（改了没变化就按 **Ctrl + F5** 强制刷新）。

---

## 操作 1：改一句话（建议先做这个练手）

**目标**：把左栏那两行身份介绍改掉。

1. 搜索 `rail:`；
2. 找到 `lines:` 那一行，长这样：

       lines: ["应用统计学 · 广东金融学院", "在读 · 寻找数据分析 / 量化分析实习"],

3. 两个引号里各是一行文字，**只改引号中间的中文**，逗号、方括号都不要动；
4. 保存 → 刷新 → 左栏两行就变了。

⚠️ 如果文字里要写英文双引号，必须写成 `\"`（反斜杠 + 引号）；写了中文引号「」是安全的，可以直接用。

---

## 操作 2：改左栏那 4 个关键数字

1. 搜索 `metrics:`（在 `rail:` 里面）；
2. 每行是一组数字 + 标签：

       { value: "100+",   label: "累计可用因子" },

3. 改 `value`（数字）和 `label`（小字说明）；
4. 保存 → 刷新。

⚠️ 如果数字口径变了（比如从「回测收益」改成别的），记得同步改下面 `metricsNote` 那句口径说明。

---

## 操作 3：加一个新项目案例

1. 搜索 `entries: [`；
2. 找 `works: [` 里面已有的项目段落，它长这样（从 `{` 到 `}` 是一整段）：

       {
         id: "SITE",
         title: "项目名称",
         ...
         todo: null
       },

3. 把这一整段**复制**，粘到它后面；
4. 修改复制出来的这一段：
   - `title` → 项目名称；
   - `meta` → 时间 · 组织 · 你的角色；
   - `role` → 你具体负责什么；
   - `background` → 为什么要做；
   - `method` → 做法，`[ "第一步", "第二步" ]`，注意**每行末尾有逗号，最后一行没有**；
   - `ledger` → 想显示表格就照抄主案例的结构；**不想显示就写 `null`**；
   - `chart` → 同理，不需要就写 `null`；
   - `results` → 结果，有数字优先；
   - `review` → 你的复盘（最加分，别空着）；
   - `tags` → 标签词；
   - `todo: null` → 还没补完就写 `todo: "待补充：xxx"`，页面会用琥珀色小字提醒；补完改回 `null`。
5. 保存 → 刷新 → 新项目出现在对应编号下面。

**提示**：如果想让新项目独立成一个编号（例如「04 数据可视化」），把它放进 `entries` 数组的一个新段落里，`id` / `no` / `title` / `lede` 都要给，并在 `nav` 里加一条同名 `id` 的目录项。

---

## 操作 4：换配色（改一个值，全站跟着变）

1. 打开 `styles.css`；
2. 找到第 1 节的 `:root { ... }`；
3. 把 `--teal: #2f9e8f;` 改成想要的颜色：

   | 想要的感觉 | 填什么 |
   |---|---|
   | 现在的偏青绿 | `#2f9e8f` |
   | 更沉稳的靛蓝 | `#4a6fa5` |
   | 克制的琥珀 | `#b8843c` |
   | 偏冷的石墨蓝 | `#5b7c8d` |

4. 保存 → 刷新 → 强调色（关键数字、标题小注、链接、曲线）一起变。

⚠️ 浅色模式的颜色在下面 `:root[data-theme="light"]` 里，要一起改才协调。

---

## 常见问题

| 现象 | 原因 | 怎么修 |
|---|---|---|
| 页面显示「内容加载失败」 | `content.js` 少了逗号 / 引号没配对 | 检查刚改的地方：**每行末尾是否有逗号、引号是否成对**；改不回来就用备份还原 |
| 改了没变化 | 浏览器缓存 | **Ctrl + F5** 强制刷新 |
| 中文变乱码 | 保存编码成了 ANSI | 「另存为」时编码选 **UTF-8** |
| 表格在手机上少一列 | 设计如此（第 3 列在窄屏隐藏） | 想保留就把 `styles.css` 里 `.ledger th:nth-child(3)` 那条规则删掉 |
| 微信二维码不显示 | 路径不对 | 确认图片在 `images/wechat-qr.png`，文件名大小写一致 |
